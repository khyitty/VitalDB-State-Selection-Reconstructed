\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{figures/controlled_experiment_framework.pdf}
    \caption{Overview of the controlled $2\times2$ experimental design. Only
    preprocessing and state representation differ across configurations, whereas
    the PPO controller, simulator, reward, training budget, random seed, and
    evaluation cases are held fixed. Color identifies preprocessing and hatching
    identifies the state factor without relying on color alone.}
    \label{fig:controlled-framework}
\end{figure*}
